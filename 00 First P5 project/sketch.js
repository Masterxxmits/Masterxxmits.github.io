// First day Demo
// Mr. Scott
// Sept. 11, 2024

// interactive Programs -> don't finish...
function setup() {
  // Happens one time at the start
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  // happens over and over, targeting 60 fps
  //background(220);
  circle(mouseX, mouseY, 20);

}
